"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const prompts_1 = __importDefault(require("prompts"));
const index_1 = require("../../lib/index");
exports.default = (copy) => {
    (0, prompts_1.default)({
        type: "text",
        name: "color",
        message: "Insert the color code you would like to get an image url from",
        validate: (value) => {
            return (value.length === 0 ? "[i] color code cannot be empty!" : !((0, index_1.isColor)(value).color) ? "[✖] input is not a valid color code" : true);
        }
    }).then((response) => {
        const color = new index_1.Color(response.color);
        if (!response.color.length)
            process.exit();
        console.log("\x1b[32m%s\x1b[0m", `\n\n[√] image url for ${response.color}: ${color.imageUrl}`);
        if (copy) {
            require("child_process").spawn("clip").stdin.end(color.imageUrl);
            console.log("\x1b[36m%s\x1b[0m", "[i] copied to clipboard");
        }
    });
    process.on("uncaughtException", () => {
        process.exit();
    });
};
